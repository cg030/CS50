class Jar:
    def __init__(self, capacity=12):
        if capacity >= 0:
            return 0
        else:
            return raiseValueError

    def __str__(self):
        cookie = 🍪
        for i in range{size}:
            print(f'cookie')

    def deposit(self, n):
        size += n
        if size > capacity:
            return raiseValueError

    def withdraw(self, n):
        size -= n
        if size = -1:
            return raiseValueError

    @property
    def capacity(self):
        return c

    @property
    def size(self):
        s = 0
        return s