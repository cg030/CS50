#include <cs50.h>
#include <stdio.h>

int numbers[] = {4,3,5,7,9,1,8};

int main(void)
{
    for(int i = 0; i < 5; i++)
    {
        if (numbers[i] > numbers[i + 1 ])
        {
            //move i to the right
            
        }
        else
    }
}