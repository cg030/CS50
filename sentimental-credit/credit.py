def main():
    # get input from user
    number = input('Number: ')

def luhn(number):
    count = 0
    sum = 0
    while(number > len(number) > 0:
        number[len(number)] # extract the last digit

        if ()